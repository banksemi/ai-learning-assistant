// Global configuration file
// This file can be modified AFTER the build process

window.config = {
  // Default API URL (can be overridden in deployment)
  // Use the value from the current .env as the default for development
  API_BASE_URL: 'http://localhost:8080/api/1'
};
